/// If you have a custom camera, you will want to modify this to get the X and Y position of your camera
/// as well as the width and height. Return this as an array of 4 values, X, Y, Width and Height. 
/// 
/// Flux needs the Top Left corner of the Camera in order to work otherwise the calculations for
/// placing the sequence will be messed up.

function flux_camera_custom_pos(){
	var _rect = [0, 0, 0, 0];
	
	
	
	return _rect;
}